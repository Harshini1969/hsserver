{
  "name": "hospital-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx json-server --watch data/server.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
